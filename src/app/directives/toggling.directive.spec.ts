import { TogglingDirective } from './toggling.directive';

describe('TogglingDirective', () => {
  it('should create an instance', () => {
    const directive = new TogglingDirective();
    expect(directive).toBeTruthy();
  });
});
